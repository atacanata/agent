# AGENTS (Repo Rules)

- File-first: source of truth is in docs/ and tasks/
- Keep changes small (single-purpose diffs)
- After changes, run local CI and write artifacts to docs/RUNBOOK/
- Never commit secrets (.env must not be committed)
